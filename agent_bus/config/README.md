# Config

Agent registry and future runtime configuration live here.

- `agents.yaml` defines the supported agent handles and their aliases
- each agent can also declare capability flags such as review, act, and comment delivery
