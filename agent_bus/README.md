# Agent Bus

This folder is the canonical handoff state for the repository.

The file system is the source of truth.

- `tasks/` contains active work requests
- `results/` contains outcome reports
- `inbox/` contains optional notification markers
- `archive/` contains closed handoffs
- `templates/` contains the canonical formats
- `docs/` contains the operating rules and lifecycle
