# Archive

Closed task/result pairs are stored here by date and task id.

Use `agentbus archive --task <path>` to move a closed pair into this tree.
Use `agentbus promote --task <archived-path>` to restore it to the active folders.
